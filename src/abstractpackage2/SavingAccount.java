package abstractpackage2;

/*
public class SavingAccount extends BankAccount {

    public SavingAccount(String accountNumber, double amount) {
        super(accountNumber, amount);
    }

    @Override
    public void deposit(double amount) {
        setAmount(getAmount() + amount);
        System.out.println("Deposit of $" + amount + " successful. currentBalance: $" + getAmount());
    }

    @Override
    public void withdraw(double amount) {
        if (getAmount() >= amount) {
            setAmount(getAmount() - amount);
            System.out.println("Withdrawal of $" + amount + " successful. Current balance: $" + getAmount());
        } else {
            System.out.println("Insufficient funds. Withdrawal failed.");
        }

    }
}
*/
